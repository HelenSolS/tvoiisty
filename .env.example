# Локальная разработка: скопируй в .env и подставь ключ. Файл .env в git не пушить.
# URL бэкенда (фронт): загрузка фото, примерка, настройки. Production — https://api.tvoiistyle.top
VITE_API_BASE_URL=https://api.tvoiistyle.top

# --- Сервер (Docker): создать .env в /opt/tvoiisty на VPS ---
# PGHOST=postgres
# PGPORT=5432
# PGUSER=tvoiisty
# PGPASSWORD=strongpassword
# PGDATABASE=tvoiisty_db
# JWT_SECRET=твой_длинный_секрет
# BLOB_READ_WRITE_TOKEN=...   # или SUPABASE_URL + SUPABASE_SERVICE_KEY + SUPABASE_BUCKET
# KIE_API_KEY=...
# FAL_KEY=...
# Backend (Vercel): в настройках проекта → Environment Variables задай KIE_API_KEY.
# Опционально: KIE_IMAGE_MODEL — точное имя модели примерки в KIE (если по умолчанию flux-2 не подходит).
# Резервная нейросеть (переключатель в настройках приложения): задай KIE_BACKUP_BASE_URL и KIE_BACKUP_API_KEY.
# Fal AI (примерка + создание промпта из картинки): один ключ для всех Fal. Имя обязательно: FAL_KEY.
# В примерочной «OpenAI» (Vision, Prompt Builder) вызывается через Fal — прямой OPENAI_API_KEY не используем.
# В Vercel: Settings → Environment Variables → Key: FAL_KEY.
# Модели Fal: fal-ai/image-apps-v2/virtual-try-on, fal-ai/nano-banana-pro/edit.
# FAL_KEY=...

# Подготовка промпта (Vision + Prompt Builder): OPENAI_API_KEY. Vision по умолчанию — gpt-4o-mini (если нет доступа к gpt-4o).
# OPENAI_API_KEY=sk-...
# OPENAI_VISION_MODEL=gpt-4o-mini

# Уведомление админа при сбоях промпта (токены, fallback): POST с { reason, stage, ts }. Например Slack/Telegram webhook.
# ADMIN_WEBHOOK_URL=https://...

# Резерв промпта: OpenAI-совместимый прокси для Fal (например fal-openai-cf-proxy). Один сброс — сразу в следующую альтернативу.
# FAL_OPENAI_PROXY_URL=https://<your-worker>.workers.dev
# FAL_PROXY_API_KEY=your_proxy_bearer_key

KIE_API_KEY=your_key_here
# KIE_IMAGE_MODEL=flux-2
# KIE_BASE_URL=https://api.kie.ai/api/v1
# KIE_BACKUP_BASE_URL=https://...
# KIE_BACKUP_API_KEY=...
