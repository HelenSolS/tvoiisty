# Локальная разработка: скопируй в .env и подставь ключ. Файл .env в git не пушить.
# Backend (Vercel): Environment Variables — KIE_API_KEY обязателен; для примерки ещё BLOB_READ_WRITE_TOKEN (Vercel Blob Store).
# Опционально: KIE_IMAGE_MODEL — имя модели примерки в KIE.

KIE_API_KEY=your_key_here
# BLOB_READ_WRITE_TOKEN=...  # создаётся в Vercel при добавлении Blob Store
# KIE_IMAGE_MODEL=flux-2/flex-image-to-image
