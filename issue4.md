# Automatic Fallback System (KIE → Fal)

## 🔒 DEV-ONLY WORKFLOW
Работать строго в ветке dev.  
Main не трогать.  
Создать feature-ветку от dev.  
Сделать PR в dev.  
Ничего не пушить в main.  
Не ломать текущую генерацию.

---

## 🎯 Цель
Если основной провайдер (KIE) возвращает ошибку — автоматически переключаться на Fal без вмешательства пользователя.

---

## 🛠 Реализация

Primary provider: KIE  
Secondary provider: Fal  

Алгоритм:

1. Отправить запрос в KIE.
2. Если:
   - timeout
   - 4xx/5xx ошибка
   - пустой или некорректный ответ
   → автоматически выполнить повторный запрос через Fal.
3. Залогировать событие fallback.
4. Вернуть пользователю результат без отображения технической ошибки.

Пример логики:
