/**
 * Skeleton подключения к Postgres.
 * На следующих шагах здесь появится пул соединений и хелперы.
 */

export interface DbClient {
  // Заглушка интерфейса клиента БД
}

export function getDbClient(): DbClient | null {
  return null;
}

